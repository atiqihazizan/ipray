# Salin ke keystore.properties (jangan commit) dan sesuaikan.
# storeFile: laluan relatif dari folder android/ (contoh: ipray-release.jks)

storeFile=ipray-release.jks
storePassword=UBAH_KATA_LALUAN_STORE
keyAlias=ipray
keyPassword=UBAH_KATA_LALUAN_KEY
